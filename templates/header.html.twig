<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container-fluid">
        <a href="{{ path('app_home') }}" style="color: #dc0000; font-size: 2rem; font-weight: 900; letter-spacing: 3px; text-transform: uppercase; text-decoration: none;">GPF1</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav ms-auto">
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_pilote_index') }}">Pilotes</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_ecurie_index') }}">Écuries</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_circuit_index') }}">Circuits</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_grand_prix_index') }}">Grand Prix</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_meteo_index') }}">Météo</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_participant_index') }}">Participants</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_voiture_index') }}">Voitures</a>
                </li>
                {# Séparateur visuel #}
                <li class="nav-item"
                    style="border-left: 1px solid rgba(255,255,255,0.15); margin-left: 0.5rem; padding-left: 0.5rem;">
                    {% if app.user %}
                    <a class="nav-link userAgo" href="#">
                        {{ app.user.prenomMembre }} {{ app.user.nomMembre }}
                    </a>
                    {% endif %}
                </li>
                <li class="nav-item">
                    {% if app.user %}
                    <a class="nav-link logout" href="{{ path('app_logout') }}">Déconnecter</a>
                    {% else %}
                    <a class="nav-link" href="{{ path('app_register') }}">S'inscrire</a>
                    <a class="nav-link logout" href="{{ path('app_login') }}">Se connecter</a>
                    {% endif %}
                </li>
                {% if is_granted('ROLE_ADMIN') %}
                <li class="nav-item">
                    <a class="nav-link" style="color: var(--f1-red) !important;"
                        href="{{ path('app_admin_membres') }}">Admin</a>
                </li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>