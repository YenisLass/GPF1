{% extends 'base.html.twig' %}

{% block title %}Edit GrandPrix{% endblock %}

{% block body %}
<div class="edit-wrapper">
    <h1>Edit GrandPrix</h1>
    {{ include('grand_prix/_form.html.twig', {'button_label': 'Update'}) }}
    <div class="action-buttons" style="margin-top: 1.5rem;">
        <a class="btn-back" href="{{ path('app_grand_prix_index') }}">← Back to list</a>
        {{ include('grand_prix/_delete_form.html.twig') }}
    </div>
</div>
{% endblock %}
